# Foundation adversarial evals

E01–E20 from `CULT4_IMPLEMENTATION_SPEC.md` are implemented as deterministic Vitest cases under `tests/foundation.test.ts`. They never call an LLM. OpenCode adapter tests use a fake runner; optional real-runtime verification is separate.
